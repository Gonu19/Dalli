# ROADMAP.md — 실행 계획 (8/14 ~ 8/20)

**마감 8/20 · 팀 5명** (고은우 Core FE / 김민서 UI FE / 김은송 BE / 디자인 2)

> 이 문서는 **누가 언제 무엇을 끝내는가**만 다룬다.
> 수치·API·스키마·문구는 `docs/*`가 단일 진실이며 여기에 다시 적지 않는다.
> `prd.md`(2026-08-12)는 플랫폼 확정 이전 문서다. **충돌 시 `docs/*`와 `AGENTS.md`가 이긴다.**

## 구조: FE 4일 스프린트 + 통합 3일

```
8/14 ~ 8/17   FE 독립 개발 4일    서버 없이 러닝 코어 완성 (Mock API)
8/18 ~ 8/19   BE 통합 2일         Mock → 실로직 교체, 야외 검증
8/20          동결 · 제출
```

**실서버 연동이 8/18에 시작**되므로, 원래 여유분이던 3일이 통합 기간으로 정확히 소진된다.
즉 **8/17까지 FE가 끝나 있다는 전제가 깨지면 남는 시간이 없다.**
컷 후보(§5)는 미루지 말고 **8/16 저녁에 확정**한다.

---

## 0. 기준 정리

### 0-1. `docs/*` 확정 사항 (PRD와 다름 — 바꾸지 않는다)

| 항목 | PRD | **확정** |
| --- | --- | --- |
| 플랫폼 | PWA, 전경 측정 (§30.1) | **Expo/iOS 네이티브, 백그라운드 측정** |
| 인증 | 이메일 + 비밀번호 (FR-001, §30.7) | **기기 UUID + JWT 30일.** 로그인·회원가입 화면 없음 |
| 목표 범위 | ±3 spm (§30.3) | **±4** (`ENGINE.md` §1) |
| 안정화 / 쿨다운 | 5초 / 30초 (§30.3) | **20초 / 60초** |
| 러닝 중 목표 조정 | 자동 변경 금지 (§30.12) | **러닝 중 다운시프트 2회 + 리커버리** (`ENGINE.md` §8·§9) |
| 첫 러닝 캘리브레이션 | — | **없음.** 실측 baseline은 종료 후 산출 (`ENGINE.md` §2) |
| 컨디션 | 1~5 | **UI 3단계 → 저장값 1 / 3 / 5** (`ENGINE.md` §3) |
| 캘린더 경로 | `/schedules` | **`/plans`** (`CONTRACT.md`) |
| 삭제 | 소프트 삭제 7일 (§30.10) | **hard delete** (`ERD.md` §8) |
| 신체 정보 | 온보딩 단계 (§12) | **PRD 유지** — 온보딩 단계로 두되 **전부 선택·건너뛰기 가능** |
| 목표 표기 | 범위 노출 | **중심값 1개만 노출** (`PRODUCT.md` §8-2) |

### 0-2. PRD에만 있어서 편입한 것

| PRD 항목 | 반영 |
| --- | --- |
| 러닝 목적 5종 온보딩 수집 (§12) | `F2-03` |
| 유효 러닝 = 활동 3분 이상 + 센서 샘플 70% 이상 (§30.5) | `BE-05` |
| 종료 확인 3분기 — 계속 / 저장 / 버리기 (FR-011) | `F2-06` |
| `UNAVAILABLE` — 품질 미달 시 판정 유보 (FR-016) | `F1-05` |
| 데이터 품질 명시, 계산 불가는 사유 표시 (FR-019) | `BE-05` `F2-07` |
| AI 리포트 출력 필드 (§20.2) | `BE-06` — `CONTRACT.md` 필드 표 7개 |
| 비정상 종료 후 세션 복구 (§18) | `F1-10` |
| 일정 연결 제안 — 전후 6시간 (§30.9) | `BE-07` |
| 권한 묶음 요청 금지 (§22) | `F1-09` |
| 결과 이미지 9:16, 경로 제외 (§30.13) | `F2-10` |
| 빈 상태에 다음 행동 1개 (§18) | `F2-11` |

---

## 1. 전제

| 항목 | 값 |
| --- | --- |
| 플랫폼 | Expo SDK 54 / iOS 실기기 (`AGENTS.md` §1) |
| 백엔드 | FastAPI + PostgreSQL 16, AWS EC2 단일 인스턴스 |
| 인증 | 기기 UUID + JWT 30일, refresh 없음 |
| 아키텍처 | **온디바이스 우선** — 러닝 중 서버 통신 없음, 종료 시 1회 업로드 |
| **실서버 제공·연동** | **8/18.** 그 전까지 BE는 기반·Mock을 완성하고 앱은 Mock을 사용 (§1-2) |
| 빌드 환경 | **맥은 김민서(UI FE) 1대뿐** (§1-1) |

### 1-1. iOS 빌드 · 기기 정책 ⚠️

맥이 한 대뿐이고 그 주인이 **센서를 담당하지 않는 사람**이라는 게 구조적 제약이다.

| 층 | 누가 · 어디서 | 무엇을 |
| --- | --- | --- |
| ① 순수 TS | 고은우 / Windows + Node | 윈도우 계산, 판정, 다운시프트, 리커버리 — **센서 없이 전부 검증** |
| ② Expo Go | 전원 / 각자 아이폰 | **전경 케이던스 측정**, 음성(`expo-speech`), 화면 |
| ③ dev build | **김민서 맥에서만** | 백그라운드 측정, 오디오 세션 믹싱, `UIBackgroundModes` |

- **③만 맥이 필요하다.** ①②로 개발의 8할이 돌아간다
- `engine/`이 순수 TS인 이유가 여기서 값을 한다. `react-native` import가 섞이면 ①이 무너진다
- **8/14에 팀 아이폰을 한 번에 등록·설치한다.** 무료 프로비저닝 7일 만료 → **8/21까지 유효**, 마감 안에 들어온다
- 코드가 바뀌면 재설치가 필요하므로 **저녁 동기화 때 재빌드 세션**을 연다 (§8)

### 1-2. Mock API는 앱 밖에, EC2에 둔다 ⚠️

**MSW처럼 앱 안에 Mock을 두지 않는다.** 8/18에 그걸 제거하는 작업 자체가 통합 리스크가 된다.

```
8/14        BE가 FastAPI Mock API를 EC2에 배포
8/14~8/17   앱은 처음부터 https://<EC2>/... 를 호출
8/18        BE가 같은 HTTPS 진입점의 업스트림을 실 API로 교체
```

**Base URL을 바꿀 일이 없다.** 앱 입장에서는 8/18에 아무 일도 일어나지 않는다.
인프라(EC2·swap 2GB·Nginx·HTTPS)와 Mock 배포는 **BE가 함께 소유**한다.
EC2 접근 권한이 제공되기 전에는 설정 절차만 확정하고, 제공 즉시 같은 인스턴스에
Mock을 배포한다. 8/18에는 Nginx의 외부 HTTPS 주소를 유지한 채 내부 업스트림만
실 API로 전환한다.

`CONTRACT.md`와 Mock은 처음부터 BE가 함께 소유해 별도 인계 없이 실 API로 전환한다.

---

## 2. 일정 개요

| 날짜 | 단계 | 게이트 (저녁까지 반드시) |
| --- | --- | --- |
| **8/14 (목) D1** | 계약 동결 · 뼈대 · Mock 배포 | **Mock API 응답** + `types.ts` 커밋 + **실기기 스파이크 3종 통과** |
| **8/15 (금) D2** | 러닝 코어 | **서버 없이 러닝 한 사이클** (시작 → 케이던스 → 종료 → Mock 업로드) |
| **8/16 (토) D3** | 판정 완성 · 화면 | **`ReplaySource`로 전체 시나리오 관통** + **컷 확정** |
| **8/17 (일) D4** | 실기기 · 오디오 · 백그라운드 | **실기기 20분 러닝 코어 검증** |
| **8/18 (월) D5** | **실서버 연동** · 실로직 교체 | **실서버 E2E 관통** |
| **8/19 (화) D6** | 야외 검증 · 통합 버그 | **야외 20분 러닝 2회 통과** |
| **8/20 (수) D7** | 동결 · 제출 | 시연 빌드 설치 + seed 주입 |

> **시연 영상·발표 자료는 이 문서 범위 밖이다.** 디자인 2명 포함해 별도로 정한다.

---

## 3. 일자별 상세

### 8/14 (D1) — 계약 동결 · 뼈대 · Mock

**전원 (오전 40분)**
- `DEMO.md` 시연 시나리오 확정 → **시연에 안 나오는 기능은 만들지 않는다**
- 화면 이름·라우트명 통일 (`SCREENS.md`)

| 담당 | 오전 | 오후 |
| --- | --- | --- |
| **Core FE** | `types.ts` `constants.ts` **선커밋** | `CadenceSource` + `ReplaySource` (Node에서 검증) |
| **UI FE** | Expo 생성 + infoPlist + **팀 기기 일괄 설치** | **스파이크 `IN-06`** → 토큰 → 온보딩 |
| **BE**\* | EC2 + swap + Nginx + HTTPS | **Mock API 배포 (`IN-02`)** |
| **디자인** | 홈·러닝 중 시안 확정 | 리포트·캘린더 시안 |

\* EC2 접근 권한이 제공되는 즉시 BE가 `IN-01`과 `IN-02`를 연속 수행한다. FE에는
외부에서 응답하는 HTTPS Base URL 하나만 전달한다.

> **오늘의 필수 산출물 3개**: 살아 있는 Mock URL / `types.ts` 커밋 / 스파이크 3종 통과.
> 스파이크를 8/17로 미루지 않는다 — 거기서 백그라운드가 안 되면 남는 시간이 3일뿐이다.

### 8/15 (D2) — 러닝 코어

| 담당 | 작업 |
| --- | --- |
| **Core FE** | Pedometer → 20초 윈도우 → 판정(`F1-05`) → `runStore` + 일시정지 |
| **UI FE** | 홈 · 러닝 준비 · 러닝 중 · 종료 확인 |

**게이트: 서버 없이 러닝 한 사이클.** 시작 → 케이던스 표시 → 종료 → Mock 업로드까지.

### 8/16 (D3) — 판정 완성 · 화면 · **컷 확정**

| 담당 | 작업 |
| --- | --- |
| **Core FE** | 다운시프트 + 리커버리 + 이벤트 → **시뮬레이션 모드(`F1-11`)** |
| **UI FE** | 리포트 · 캘린더 (Mock 데이터) |

**게이트: `ReplaySource`로 [과속 → 안정 → 저하 → 실패 2회 → 하향 → 완주] 전체 관통.**

> **오늘 저녁에 §5 컷 리스트를 확정한다.** 8/17로 미루면 이미 만든 걸 못 버린다.
> 저녁에 **5분짜리 야외 테스트 1회** — 판정 로직이 완성된 시점이라 실측 케이던스를 여기서 봐야 한다.

### 8/17 (D4) — 실기기 · 오디오 · 백그라운드

| 담당 | 작업 |
| --- | --- |
| **Core FE** | 오디오 세션 + 무음 루프 + GPS + 권한 + 세션 복구 |
| **UI FE** | 분석 목록 · 빈 상태 · 오류 · 권한 화면 |

**게이트: 실기기에서 20분 러닝 코어 검증.** 음악 재생 + 화면 끄고 백그라운드 생존 확인.

**FE 스프린트는 여기서 끝난다.** 8/18부터는 통합과 버그 대응이다.

### 8/18 (D5) — 실서버 연동 · 실로직 교체

| 담당 | 작업 |
| --- | --- |
| **BE** | `dalli_test` 마이그레이션 검증 → 실 DB 연결 → `/auth/device` → `POST /runs` |
| **Core FE** | 업로드 재시도 큐 실서버 검증 · 통합 버그 |
| **UI FE** | 통합 버그 · 밀린 화면 마감 |

**Base URL은 바뀌지 않는다.** 같은 Nginx 업스트림 포트의 Mock 컨테이너를 실 API 컨테이너로 교체한다 (§1-2).

### 8/19 (D6) — 야외 검증 · 통합

| 담당 | 작업 |
| --- | --- |
| **BE** | `metrics.py`(RS·LDR·FI) → **리포트 폴백** → 조회 API → `/plans` `/calendar` `/stats` |
| **FE** | **야외 20분 러닝 2회** + 수정 |

**야외 2회는 서로 다른 조건으로.** 1회는 음악 + 화면 끄고, 1회는 신호대기 있는 도심 코스로.
백그라운드 사망과 정지 판정이 여기서 갈린다.

### 8/20 (D7) — 동결 · 제출

| 시간 | 작업 | 담당 |
| --- | --- | --- |
| 오전 | 잔여 버그 · `seed.py` · **신규 기능 금지** | 전원 |
| 오후 | `DEMO.md` 리허설 체크리스트 전 항목 통과 | 고은우 · 김은송 |
| 마감 3h 전 | **시연용 빌드 재설치 + seed 주입 + 제출** | 김민서 |

**제출을 마감 직전에 하지 않는다.**

---

## 4. 작업 목록

담당: `F1` 고은우(Core FE) · `F2` 김민서(UI FE) · `BE` 김은송

### 인프라 · 계약

| ID | 작업 | 담당 | 날짜 | 완료 조건 |
| --- | --- | --- | --- | --- |
| `IN-01` | EC2 + swap 2GB + Nginx + Certbot | BE | 접근 제공 즉시 | HTTPS로 `/health` 200 |
| `IN-02` | **Mock API EC2 배포** (동일 HTTPS Base URL) | BE | IN-01 직후 | `CONTRACT.md`대로 전 엔드포인트 응답 |
| `IN-03` | Expo 프로젝트 + infoPlist + **팀 기기 일괄 설치** | F2 | 8/14 | 팀 아이폰 전부에 dev build 설치 |
| `IN-04` | OpenAPI → `types/api.ts` 자동 생성 | F2 | 8/18 | 실서버 스키마로 타입 갱신 |
| `IN-05` | docker compose + Alembic | BE | 8/18 | `dalli_test`에서 upgrade/downgrade/re-upgrade와 통합 테스트 통과 |
| `IN-06` | **실기기 스파이크** (Pedometer + 무음루프 + `expo-speech` 각 1줄) | F2 | **8/14** | §6 확인 3가지 통과. 30분 |

### 엔진 (Core FE)

| ID | 작업 | 선행 | 날짜 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `F1-01` | `types.ts` + `constants.ts` **선커밋** | — | 8/14 | UI FE가 목 데이터로 착수 가능 | — |
| `F1-02` | `CadenceSource` + `ReplaySource` | F1-01 | 8/14 | **Node에서** 배열 재생만으로 판정이 돌아감 | — |
| `F1-03` | Pedometer 누적값 1초 샘플링 → 20초 윈도우 | IN-03 | 8/15 | 실기기 걷기에서 SPM 표시 | FR-009 |
| `F1-05` | 판정 4상태 + 워밍업 90초 | F1-03 | 8/15 | `ENGINE.md` §5 타임라인대로. 품질 미달 시 `UNAVAILABLE` | FR-012, 013, 016 |
| `F1-07` | `runStore` + 일시정지·재개 | F1-03 | 8/15 | pause 중 시간·지표 정지 | FR-010 |
| `F1-06` | **다운시프트 2회 + 리커버리** | F1-05 | 8/16 | 하향 2회 후 3번째 조건 → 리커버리, 이후 무음 | FR-014 |
| `F1-11` | **시뮬레이션 모드 토글** | F1-02, F1-06 | 8/16 | 전체 시나리오 재생 | — |
| `F1-04` | 오디오 세션 + 무음 루프 백그라운드 | IN-03 | 8/17 | **음악 재생 중 화면 꺼도 측정 지속** | FR-017 |
| `F1-08` | GPS 거리·페이스 + 품질 판정 | IN-03 | 8/17 | 미수신 시 `null`, 케이던스는 정상 | FR-016 |
| `F1-09` | 권한 요청 (모션·위치·사진) | IN-03 | 8/17 | **시점별 개별 요청.** 거부해도 제한 모드 | FR-027 |
| `F1-10` | 로컬 저장 + 재시도 큐 + **세션 복구** | IN-02 | 8/17 | 비행기 모드 종료 → 중복 없이 동기화 | FR-017, 029 |

> **엔진은 `Date.now()`/`setTimeout`으로 시간을 재지 않는다** (`ENGINE.md` §6).
> 모든 시간 판정은 tick의 `elapsedSec` 기준. 이게 깨지면 `ReplaySource` 배속이 무의미해진다.

### 화면 (UI FE)

| ID | 작업 | 선행 | 날짜 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `F2-01` | 디자인 토큰 → `theme/tokens.ts` | — | 8/14 | 색·타이포 상수화 | — |
| `F2-02` | 시작 화면 + 기기 UUID 자동 인증 | IN-02 | 8/14 | 첫 실행 시 로그인 없이 온보딩 진입 | FR-001 대체 |
| `F2-03` | 온보딩 (목적 5종·경험·**신체 정보**·목표·주간횟수·기준 리듬) | IN-02 | 8/14 | 신체 정보는 **전부 건너뛰기 가능** | FR-002~006 |
| `F2-04` | 홈 + 러닝 준비 | F1-01 | 8/15 | 컨디션 3단계 → 서버 전송값 5/3/1 | FR-007, 008 |
| `F2-05` | 러닝 중 화면 (**리듬 중심값 1개** · 색 상태) | F1-01 | 8/15 | 범위 숫자 노출 금지 | FR-012 |
| `F2-06` | 종료 확인 (계속 / 저장 / 버리기) | F1-07 | 8/15 | 버튼 1회로 삭제되지 않음 | FR-011 |
| `F2-07` | 기본 리포트 + AI 리포트 (준비·지연·실패) | IN-02 | 8/16 | **AI 실패해도 기본 리포트 열람 가능** | FR-018~021 |
| `F2-09` | 기록 탭: 캘린더 + 일정 + 수기 기록 | IN-02 | 8/16 | 계획·완료·미완료·수기를 **라벨+형태**로 구분 | FR-023, 024 |
| `F2-08` | 분석 목록 + 러닝 상세 | IN-02 | 8/17 | 삭제 시 캘린더·달리 데이 동시 갱신 | FR-022 |
| `F2-11` | 빈 상태 · 로딩 · 오류 · 권한 거부 | — | 8/17 | 각 상태에 **다음 행동 1개** | FR-028 |
| `F2-10` | 결과 이미지 (갤러리 선택 → 9:16 → 저장) | F2-06 | **컷 후보** | 취소·거부·실패에도 러닝 유지 | FR-031 |

### 서버 (BE — 8/18 실서버 연동)

| ID | 작업 | 선행 | 날짜 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `BE-01` | 모델 + Alembic → `dalli_test` 검증 | IN-02, IN-05 | 8/18 | `ERD.md`대로 테스트 DB 마이그레이션 적용 | — |
| `BE-02` | `POST /auth/device` — 기기 UUID → JWT | BE-01 | 8/18 | 같은 UUID 재요청 시 동일 사용자 | FR-001 대체 |
| `BE-03` | `GET/PATCH /users/me` | BE-02 | 8/18 | `onboarded` 서버 계산 | FR-002, 025 |
| `BE-04` | `POST /runs` + **`client_run_id` 멱등** | BE-01 | 8/18 | 같은 키 재전송 시 200 + 기존 레코드 | FR-018, 029 |
| `BE-05` | `metrics.py` — RS·LDR·FI + 유효 러닝(3분·70%) | BE-04 | 8/19 | `ENGINE.md` §12대로. 6분 미만은 `null` + 사유 | FR-019 |
| `BE-06` | `POST /runs/{run_id}/report` — **폴백 먼저** | BE-05 | 8/19 | LLM 죽어도 **200 + 폴백**, 8초 타임아웃 | FR-020, 021 |
| `BE-09` | `GET /runs` · `/runs/{run_id}` · `DELETE` | BE-04 | 8/19 | 목록에 `samples` 미포함 | FR-022 |
| `BE-07` | `/plans` CRUD + `/calendar` | BE-01 | 8/19 | 전후 6시간 일정 연결 제안 → `DONE` | FR-023 |
| `BE-08` | `/stats` — 달리 데이 · 누적 활동일 | BE-04 | 8/19 | 수기 기록이 달리 데이를 올리지 않음 | FR-024 |
| `BE-10` | `seed.py` — 데모용 과거 기록 | BE-04 | 8/20 | 시연 계정에 리포트·캘린더가 채워짐 | — |
| — | **LLM 실제 연동** | BE-06 | **컷 후보** | 안 되면 폴백 문구로 시연 | — |

**`BE-06`은 폴백을 먼저 만든다.** 룰베이스가 동작한 뒤에 LLM을 붙인다.
AI 출력 필드는 `CONTRACT.md`의 표 7개가 전부이며 그 구조를 JSON schema로 강제한다.

---

## 5. 컷 리스트 — **8/16 저녁에 확정**

실서버 연동이 8/18에 시작되므로 슬랙이 없다. 미루지 말고 위에서부터 순서대로 잘라낸다.

1. **`F2-10` 결과 이미지** — 러닝 MVP 검증과 무관. 1순위 컷
2. **LLM 실제 호출** — 폴백 문구로 리포트가 완성되게 이미 설계됨
3. 분석 탭 추이 그래프 → 숫자 목록만
4. `F1-10` 세션 복구 → 재시도 큐만 남기고 복구 시트 생략
5. `F2-09` 수기 기록 → 캘린더 조회만
6. 지도·경로 표시
7. 설정 화면 → 진입점만 두고 내부 생략

**절대 자르지 않는 것**
온보딩 → 홈 → 러닝 → 케이던스 측정 → 이탈 판정 → 개입 → **Downshift** → 종료 → 저장 → 기본 리포트 → 캘린더 → **시뮬레이션 모드**

---

## 6. 리스크와 폴백

| 리스크 | 감지 | 폴백 |
| --- | --- | --- |
| 백그라운드 Pedometer 정지 | **8/14 (`IN-06`)** | 무음 루프 → 안 되면 전경 측정 + 화면 켜둔 채 시연 |
| 음악 위 오디오 믹싱 실패 | **8/14 (`IN-06`)** | 음성만 사용, 메트로놈 기본 꺼짐 |
| **BE 8/18 착수가 더 밀림** | 8/18 | **Mock을 그대로 유지**하고 시연. 앱은 차이를 모른다 (§1-2) |
| 맥 1대 병목 — 김민서 부재 | 상시 | ①②층으로 개발 계속, dev build 필요 작업만 대기 (§1-1) |
| Core FE 센서 실측 불가 | 상시 | `ReplaySource` + 자기 아이폰 Expo Go 전경 측정 |
| 8/17 FE 스프린트 미완 | 8/17 저녁 | §5 컷 1~4 즉시 적용. 8/18 통합에 손대지 않는다 |
| 야외에서 대량 버그 | 8/19 | 실센서 경로 손대지 않고 **시뮬레이션 모드로 시연 가능한 상태**만 확보 |

**8/14 오후에 확인할 3가지** — 통과되면 나머지 일정이 성립한다.
1. 음악 재생 중 화면 끄고 걸었을 때 케이던스가 계속 잡히는가
2. 음성·클릭음이 음악 위에 얹히는가 (음악이 멈추지 않는가)
3. Mock API URL이 외부에서 응답하는가

---

## 7. 개발 산출물

| 항목 | 마감 | 담당 |
| --- | --- | --- |
| 시연용 dev build (팀 기기 설치 완료) | 8/20 마감 3h 전 | 김민서 |
| seed 데이터 주입된 시연 계정 | 8/20 마감 3h 전 | 김은송 |
| 설치 링크 (선택) | 8/20 마감 3h 전 | 김민서 |

설치 링크는 **시뮬레이션 모드가 동작할 때만** 낸다.

**시연 영상·발표 자료는 이 표에 없다.** 디자인 2명 포함해 별도 논의로 정한다.

---

## 8. 매일 지키는 것

- 아침 10분 · 저녁 10분 동기화. 저녁엔 반드시 `main` 머지
- **저녁 동기화 직후 김민서가 재빌드·재설치 세션**을 연다 (§1-1)
- `git pull --rebase && git push` — **빌드 깨진 채 push 금지**
- 문서와 코드가 다르면 **문서가 맞다.** 수치는 `ENGINE.md`부터 고친다
- 남의 폴더는 직접 고치지 않고 주인에게 요청 (`AGENTS.md` §2)
