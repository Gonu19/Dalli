# ROADMAP.md — 4일 실행 계획

**기간 4일 · 팀 5명** (고은우 Core FE / 김민서 UI FE / 김은송 BE / 디자인 2)

> 이 문서는 **누가 언제 무엇을 끝내는가**만 다룬다.
> 수치·API·스키마·문구는 `docs/*`가 단일 진실이며 여기에 다시 적지 않는다.
> `prd.md`(2026-08-12)는 플랫폼 확정 이전 문서다. **충돌 시 `docs/*`와 `AGENTS.md`가 이긴다.**

---

## 0. 기준 정리

### 0-1. `docs/*` 확정 사항 (PRD와 다름 — 바꾸지 않는다)

| 항목 | PRD | **확정** |
| --- | --- | --- |
| 플랫폼 | PWA, 전경 측정 (§30.1) | **Expo/iOS 네이티브, 백그라운드 측정** |
| 인증 | 이메일 + 비밀번호 (FR-001, §30.7) | **기기 UUID + JWT 30일.** 로그인·회원가입 화면 없음 |
| 목표 범위 | ±3 spm (§30.3) | **±4** (`ENGINE.md` §1) |
| 안정화 / 쿨다운 | 5초 / 30초 (§30.3) | **20초 / 60초** |
| 러닝 중 목표 조정 | 자동 변경 금지 (§30.12) | **러닝 중 다운시프트 2회 + 리커버리** (`ENGINE.md` §8·§9) |
| 캘린더 경로 | `/schedules` | **`/plans`** (`CONTRACT.md`) |
| 삭제 | 소프트 삭제 7일 (§30.10) | **hard delete** (`ERD.md` §8) |
| 신체 정보 | 온보딩 단계 (§12) | **PRD 유지** — 온보딩 단계로 두되 **전부 선택·건너뛰기 가능**. 설정에서 보완 |
| 목표 표기 | 범위 노출 | **중심값 1개만 노출** (`PRODUCT.md` §8-2) |

러닝 중 하향을 유지하는 이유: 기획서 기능1의 *"회복이 어렵다면 무리 없는 목표 범위로 조정"*이
Runo·NRC와의 차별점이고 `DEMO.md` 45–60초 구간의 클라이맥스다. 구현 비용은 상태 변수 3개 수준이다.

### 0-2. PRD에만 있어서 새로 편입한 것

`docs/*`에 없던 항목이라 이번에 작업 목록에 반영했다.

| PRD 항목 | 반영 |
| --- | --- |
| 러닝 목적 5종 (완주·습관·체중·체력·기록) 온보딩 수집 (§12) | `F2-03` |
| 유효 러닝 = 활동 3분 이상 + 센서 샘플 70% 이상 (§30.5) | `BE-05` |
| 종료 확인 3분기 — 계속 / 저장 / 버리기 (FR-011) | `F2-06` |
| `UNAVAILABLE` — 센서 품질 미달 시 빠름/느림 **판정 유보** (FR-016) | `F1-05` |
| 데이터 품질 명시, 계산 불가 항목은 사유 표시 (FR-019) | `BE-05` `F2-07` |
| AI 출력 필드 — 판정·**근거**·가설·처방·다음 목표·**회복 안내**·`limitation` (§20.2) | `BE-06` |
| 비정상 종료 후 세션 복구 — 재개 / 저장 / 버리기 (§18) | `F1-10` |
| 일정 연결 제안 — 전후 6시간 첫 측정 러닝 1개만 (§30.9) | `BE-07` |
| 권한 묶음 요청 금지, 시점별 개별 요청 (§22) | `F1-09` |
| 결과 이미지 9:16, 시간·거리·평균 케이던스 포함, 경로 제외 (§30.13) | `F2-10` |
| 빈 상태에 다음 행동 1개 제시 (§18) | `F2-11` |

---

## 1. 전제

| 항목 | 값 |
| --- | --- |
| 플랫폼 | Expo SDK 54 / iOS 실기기 (`AGENTS.md` §1) |
| 백엔드 | FastAPI + PostgreSQL 16, AWS EC2 단일 인스턴스 |
| 인증 | 기기 UUID + JWT 30일, refresh 없음 |
| 아키텍처 | **온디바이스 우선** — 러닝 중 서버 통신 없음, 종료 시 1회 업로드 |
| 리포트 | 기본 리포트는 `POST /runs` 응답으로 즉시 / AI는 별도 호출 |
| 데모 | `ReplaySource` 시뮬레이션 모드 — **D1에 착수** |
| **빌드 환경** | **맥은 김민서(UI FE) 1대뿐.** iOS dev build·실기기 설치는 전부 이 맥을 경유 |

### 1-1. iOS 빌드 · 기기 정책 ⚠️

맥이 한 대뿐이고 그 주인이 **센서를 담당하지 않는 사람**이라는 게 이 프로젝트의 구조적 제약이다.
Core FE(고은우)가 Pedometer·오디오 세션·백그라운드 생존을 만드는데 정작 실기기 빌드를 못 굽는다.

**대응 3단**

| 층 | 누가 · 어디서 | 무엇을 |
| --- | --- | --- |
| ① 순수 TS | 고은우 / Windows + Node | 윈도우 계산, 판정, 다운시프트, 리커버리 — **센서 없이 전부 검증** |
| ② Expo Go | 전원 / 각자 아이폰 | **전경 케이던스 측정**, 음성(`expo-speech`), 화면. dev build 불필요 |
| ③ dev build | **김민서 맥에서만** | 백그라운드 측정, 오디오 세션 믹싱, `UIBackgroundModes` |

- **③만 맥이 필요하다.** ①②로 개발의 8할이 돌아가므로 병목은 생각보다 작다
- `engine/`이 순수 TS인 이유가 여기서 값을 한다. `react-native` import가 섞이면 ①이 무너진다

**기기 등록·설치**
- **D1에 팀 아이폰을 한 번에 김민서 맥에 꽂아 등록·설치한다.** 나중에 한 명씩 추가하면 매번 맥을 빌려야 한다
- 무료 프로비저닝은 **7일 만료**다. D1 설치 → D4 시연까지 안에서 끝난다
- 코드가 바뀌면 재설치가 필요하므로 **저녁 동기화 때 김민서가 재빌드·재설치 세션을 연다** (§8)
- 설치만 끝나면 야외 테스트는 누구든 뛸 수 있다. 뛰는 사람이 맥을 들고 다닐 필요는 없다

**김민서 과부하 주의**: 빌드 담당 + UI 화면 전체를 혼자 진다.
D3~D4에 작업이 몰리므로 **`F2-11`(빈 상태·오류 화면)과 `F2-10`(결과 이미지)은
다른 화면이 끝난 뒤 착수**하고, 밀리면 §5 컷 리스트를 먼저 적용한다.

---

## 2. 4일 개요

| 일자 | 주제 | 게이트 (저녁까지 반드시) |
| --- | --- | --- |
| **D1** | 계약 동결 · 뼈대 · 더미 API | 더미 API URL 응답 + `engine/types.ts` 커밋 + 실기기 설치 |
| **D2** | 러닝 코어 | 실기기에서 시작 → 케이던스 표시 → 종료 → 서버 저장 |
| **D3** | 리포트 · 캘린더 · 종단 | 첫 실행 → 온보딩 → 러닝 → 리포트 → 캘린더가 실서버로 1회 관통 |
| **D4** | 야외 검증 · 안정화 · 빌드 | 야외 20분 러닝 2회 통과 + 시연용 빌드 설치 완료 |

> **시연 영상·발표 자료는 이 문서 범위 밖이다.** 디자인 2명 포함해 별도로 정한다.
> 이 로드맵은 개발 산출물만 다룬다.

**D3 저녁 게이트를 못 넘으면 D4 오전에 기능을 잘라낸다.** §5 순서대로.

---

## 3. 일자별 상세

### D1 — 계약 동결 · 뼈대

**전원 (오전 40분)**
- `DEMO.md` 시연 시나리오 확정 → **시연에 안 나오는 기능은 만들지 않는다** (스코프 커팅 기준)
- 화면 이름·라우트명 통일 (`SCREENS.md`)

| 담당 | 오전 | 오후 |
| --- | --- | --- |
| **BE** | EC2 + swap 2GB + Nginx + docker compose | **더미 응답 EC2 배포(최우선)** → 모델 + Alembic + `/auth/device` |
| **Core FE** | `types.ts` `constants.ts` 커밋 | `CadenceSource` + `ReplaySource` (Node에서 검증) |
| **UI FE** | Expo 생성 + `app.json` infoPlist + **팀 기기 일괄 등록·설치** | **스파이크 검증(`IN-06`)** → 디자인 토큰 → 온보딩 퍼블리싱 |
| **디자인** | 홈·러닝 중 시안 확정 | 리포트·캘린더 시안 |

> **BE의 D1 유일한 필수 산출물은 "살아 있는 더미 API URL"이다.** DB 연동보다 우선한다.
> **Core FE는 `types.ts`를 구현 전에 먼저 커밋한다.** UI FE가 하루도 기다리지 않는다.
> **Expo 세팅과 기기 설치는 맥을 가진 김민서가 맡는다** (§1-1). 고은우는 그 시간에 엔진 로직을 짠다.

### D2 — 러닝 코어

| 담당 | 작업 |
| --- | --- |
| **Core FE** | Pedometer → 20초 윈도우 → 판정 → 오디오 세션 + 백그라운드 무음 루프 |
| **UI FE** | 러닝 준비 · 러닝 중 · 종료 확인 화면 + 실 API 연동 교체 |
| **BE** | `POST /runs` + `metrics.py`(RS·LDR·FI) + `GET /users/me` |

**D2 저녁에 반드시 밖에 나가서 5분 뛴다.** 백그라운드 사망·음악 겹침·GPS를 여기서 처음 본다.
D4에 처음 확인하면 고칠 시간이 없다.

> 뛰기 전에 **김민서가 재빌드·재설치 세션을 연다**(§1-1). D1에 설치가 끝났어도 코드가 바뀌었으면 다시 깔아야 한다.
> 설치만 되면 뛰는 사람은 누구든 상관없다.

### D3 — 리포트 · 캘린더 · 종단

| 담당 | 작업 |
| --- | --- |
| **Core FE** | 다운시프트 + 리커버리 + 이벤트 기록 → **시뮬레이션 모드 완성** |
| **UI FE** | 리포트 · 분석 목록 · 캘린더 · 결과 이미지 |
| **BE** | 리포트 **폴백 먼저** → LLM → `/plans` `/calendar` `/stats` → `seed.py` |

**게이트: 첫 실행 → 온보딩 → 러닝 → 리포트 → 캘린더가 실서버로 한 번 관통.**

### D4 — 야외 검증 · 안정화 · 빌드

| 시간 | 작업 | 담당 |
| --- | --- | --- |
| 오전 | 재설치 → **야외 20분 러닝 2회** + 긴급 수정. **신규 기능 금지** | 전원 |
| 오후 | 야외에서 나온 버그 수정 · 엣지 케이스 화면 마감 | 전원 |
| 오후 | `DEMO.md` 리허설 체크리스트 전 항목 통과 | 고은우 · 김은송 |
| 마감 3h 전 | **시연용 빌드 재설치 + seed 데이터 주입** | 김민서 |

**야외 러닝 2회는 서로 다른 조건으로 한다.** 1회는 음악 재생 + 화면 끄고,
1회는 신호대기가 있는 도심 코스로. 백그라운드 사망과 정지 판정이 여기서 갈린다.

시연 영상·발표 자료는 별도로 정한다 (§2).

**제출을 마감 직전에 하지 않는다.** 링크가 비어 있는 사고가 가장 흔하다.

---

## 4. 작업 목록

담당: `F1` 고은우(Core FE) · `F2` 김민서(UI FE) · `BE` 김은송

### 인프라 · 계약

| ID | 작업 | 담당 | 일자 | 완료 조건 |
| --- | --- | --- | --- | --- |
| `IN-01` | EC2 + swap 2GB + Nginx + Certbot | BE | D1 | HTTPS로 `/health` 200 |
| `IN-02` | **더미 응답 전 엔드포인트 배포** | BE | D1 | 프론트가 `CONTRACT.md`대로 호출 성공 |
| `IN-03` | docker compose (db 5432 루프백) + Alembic | BE | D1 | `alembic upgrade head` 성공 |
| `IN-04` | Expo 프로젝트 + `app.json` infoPlist + **팀 기기 일괄 등록·설치** | **F2** | D1 | 팀 아이폰 전부에 dev build 설치 완료 (맥 1회 세션) |
| `IN-05` | OpenAPI → `types/api.ts` 자동 생성 | F2 | D1 | 명령 한 줄로 타입 갱신 |
| `IN-06` | **스파이크 검증 앱** (엔진 없이 Pedometer + 무음루프 + `expo-speech` 각 1줄) | **F2** | D1 | §6의 확인 3가지를 실기기로 통과. 30분 작업 |

### 엔진 (Core FE)

| ID | 작업 | 선행 | 일자 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `F1-01` | `types.ts` + `constants.ts` **선커밋** | — | D1 | UI FE가 목 데이터로 화면 착수 가능 | — |
| `F1-02` | `CadenceSource` + `ReplaySource` | F1-01 | D1 | 배열 재생만으로 판정이 돌아감 (센서 불필요) | — |
| `F1-03` | Pedometer 누적값 1초 샘플링 → 20초 윈도우 SPM | IN-04 | D2 | 실기기 걷기에서 SPM 표시 | FR-009 |
| `F1-04` | 오디오 세션 `MixWithOthers` + 무음 루프 백그라운드 | IN-04 | D2 | **음악 재생 중 화면 꺼도 측정 지속** | FR-017 |
| `F1-05` | 판정 4상태 + 워밍업 90초 | F1-03 | D2 | `ENGINE.md` §5 타임라인대로. **품질 미달 시 `UNAVAILABLE`로 판정 유보** | FR-012, 013, 016 |
| `F1-06` | **다운시프트 2회 + 리커버리 모드** | F1-05 | D3 | 하향 2회 후 3번째 조건 → 리커버리, 이후 무음 | FR-014 |
| `F1-07` | `runStore` 상태머신 + 일시정지·재개 | F1-03 | D2 | pause 중 시간·지표 정지, 재개 시 동일 세션 | FR-010 |
| `F1-08` | GPS 거리·페이스 + 품질 판정 | IN-04 | D2 | 미수신 시 `null`, 케이던스는 정상 동작 | FR-016 |
| `F1-09` | 권한 요청 (모션·위치·사진) | IN-04 | D2 | **시점별 개별 요청**(묶음 금지). 거부해도 제한 모드 진입 | FR-027 |
| `F1-10` | 로컬 저장 + 재시도 큐 + **세션 복구** | BE-04 | D3 | 비행기 모드 종료 → 중복 없이 동기화. 강제 종료 후 재실행 시 **재개/저장/버리기** | FR-017, 029 |
| `F1-11` | **시뮬레이션 모드 토글** | F1-02, F1-06 | D3 | 60초에 [이탈→개입→하향→완주] 전 서사 재생 | — |

### 화면 (UI FE)

| ID | 작업 | 선행 | 일자 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `F2-01` | 디자인 토큰 → `theme/tokens.ts` | — | D1 | 색·타이포 상수화 | — |
| `F2-02` | 시작 화면 + **기기 UUID 자동 인증** | IN-02 | D1 | 첫 실행 시 로그인 없이 온보딩 진입 | FR-001 대체 |
| `F2-03` | 온보딩 (**목적 5종**·경험·지속시간·**신체 정보**·목표·주간횟수·기준 리듬) | IN-02 | D1 | 재실행 시 완료 단계 보존. **신체 정보는 전부 건너뛰기 가능**, 사용 목적 사전 표시 | FR-002~006 |
| `F2-04` | 홈 + 러닝 준비 (컨디션·목표·권한 상태) | F1-01 | D2 | 목표 없음/있음 두 상태 모두 시작 가능 | FR-007, 008 |
| `F2-05` | 러닝 중 화면 (시간·거리·**리듬 중심값 1개**·색 상태) | F1-01 | D2 | 범위 숫자 노출 금지 (`PRODUCT.md` §8-2) | FR-012 |
| `F2-06` | 종료 확인 (계속 / 저장 / 버리기) | F1-07 | D2 | 버튼 1회로 삭제되지 않음, 버리기는 재확인 | FR-011 |
| `F2-07` | 기본 리포트 + AI 리포트 (준비·지연·실패 3상태) | BE-05, BE-06 | D3 | **AI 실패해도 기본 리포트 열람 가능.** 계산 불가 항목은 사유 표시 | FR-018~021 |
| `F2-08` | 분석 목록 + 러닝 상세 | BE-09 | D3 | 삭제 시 캘린더·달리 데이 동시 갱신 | FR-022 |
| `F2-09` | 기록 탭: 캘린더 + 일정 추가 + 수기 기록 | BE-07 | D3 | 계획·완료·미완료·수기를 **라벨+형태**로 구분 | FR-023, 024 |
| `F2-10` | 결과 이미지 (갤러리 선택 → 9:16 합성 → 기기 저장) | F2-06 | D3 | 시간·거리·평균 케이던스 포함, **경로 제외**. 취소·거부·실패에도 러닝 유지 | FR-031 |
| `F2-11` | 빈 상태 · 로딩 · 오류 · 권한 거부 화면 | — | D3 | 각 상태에 **다음 행동 1개** 제시 | FR-028 |

> `F2-10`은 **카메라 촬영 대신 `expo-image-picker` 갤러리 선택만** 구현한다.
> 권한·프리뷰·전후면 전환이 통째로 사라진다.

### 서버 (BE)

| ID | 작업 | 선행 | 일자 | 완료 조건 | FR |
| --- | --- | --- | --- | --- | --- |
| `BE-01` | 모델 + Alembic (`users`/`runs`/`reports`/`plans`) | IN-03 | D1 | `ERD.md`대로 마이그레이션 적용 | — |
| `BE-02` | `POST /auth/device` — 기기 UUID → JWT 30일 | BE-01 | D1 | 같은 UUID 재요청 시 동일 사용자 반환 | FR-001 대체 |
| `BE-03` | `GET/PATCH /users/me` (온보딩·프로필 공용) | BE-02 | D2 | `onboarded` 플래그 서버 계산 | FR-002, 025 |
| `BE-04` | `POST /runs` + **`client_run_id` 멱등** | BE-01 | D2 | 같은 키 재전송 시 200 + 기존 레코드 | FR-018, 029 |
| `BE-05` | `metrics.py` — RS · LDR · FI + **유효 러닝(3분·70%)** | BE-04 | D2 | `ENGINE.md` §12 정의대로. 6분 미만은 `null` + 사유 | FR-019 |
| `BE-06` | `POST /runs/{run_id}/report` — **폴백 먼저, LLM 나중** | BE-05 | D3 | LLM 죽어도 **200 + 폴백**, 8초 타임아웃. **`CONTRACT.md` 출력 필드 7개 구조 강제** | FR-020, 021 |
| `BE-07` | `/plans` CRUD + `/calendar` 통합 조회 | BE-01 | D3 | 러닝 저장 시 **전후 6시간 일정 1개** 연결 제안 → `DONE` 전환 | FR-023 |
| `BE-08` | `/stats` — 달리 데이 · 누적 활동일 | BE-04 | D3 | 수기 기록이 달리 데이를 올리지 않음, 같은 날 2회 = 1일 | FR-024 |
| `BE-09` | `GET /runs` · `GET /runs/{id}` · `DELETE /runs/{id}` | BE-04 | D3 | 목록에 `samples` 미포함 | FR-022 |
| `BE-10` | `seed.py` — 데모용 과거 기록 | BE-04 | D3 | 시연 계정에 리포트·캘린더가 채워짐 | — |

**`BE-06`은 폴백을 먼저 만든다.** 룰베이스 문구가 동작한 뒤에 LLM을 붙인다.
순서를 뒤집으면 LLM이 늦어질 때 리포트 화면이 통째로 빈다.

AI 출력 필드는 `CONTRACT.md`의 표 7개가 전부다. 그 구조를 JSON schema로 강제하고,
*"~때문이다"* 대신 *"~의 영향일 수 있어요"*로 가설임을 명시한다 (PRD §20.3).

---

## 5. 컷 리스트 (위에서부터 순서대로 버린다)

D3 저녁 게이트를 못 넘으면 이 순서로 잘라낸다.

1. `F2-10` 결과 이미지
2. 분석 탭 그래프 → 숫자 목록만
3. `F2-09` 수기 기록 → 캘린더 조회만
4. 지도·경로 표시 (PRD에 있으나 4일 범위 밖)
5. `BE-06` LLM → 폴백 문구만으로 시연
6. 설정 화면 → 진입점만 두고 내부 생략

**절대 자르지 않는 것**
케이던스 측정 → 개입 → 다운시프트 → 리포트 → 캘린더 기록 → 시뮬레이션 모드

---

## 6. 리스크와 폴백

| 리스크 | 감지 시점 | 폴백 |
| --- | --- | --- |
| 백그라운드에서 Pedometer 정지 | **D1 오후** (`IN-06`) | 무음 루프(`F1-04`) → 안 되면 전경 측정 + 화면 켜둔 채 시연 |
| 음악 위 오디오 믹싱 실패 | **D1 오후** (`IN-06`) | 음성만 사용, 메트로놈 기본 꺼짐 |
| **맥 1대 병목 — 김민서 부재·장비 장애** | 상시 | ①②층(순수 TS·Expo Go)으로 개발 계속. dev build 필요 작업만 대기 (§1-1) |
| **Core FE가 센서 실측 불가** | D1~ | `ReplaySource`로 로직 검증 + 자기 아이폰 Expo Go로 전경 측정 확인 |
| Apple 프로비저닝 만료·지연 | D1 | Expo Go로 개발 지속(`ReplaySource`) + Android APK로 제출 링크 확보 |
| EC2·LLM 장애 | 상시 | 온디바이스 우선 구조라 러닝은 무사. 프론트 하드코딩 리포트 폴백 |
| D3 종단 연결 실패 | D3 저녁 | §5 컷 리스트 1~3 즉시 적용 |
| 야외 테스트에서 대량 버그 | D4 오전 | 실센서 경로 손대지 않고 **시뮬레이션 모드로 시연 가능한 상태**만 확보 |

**D1 오후에 확인할 3가지** — 통과되면 나머지는 일정 안에 들어온다.
1. 음악 재생 중 화면 끄고 걸었을 때 케이던스가 계속 잡히는가
2. 음성·클릭음이 음악 위에 얹히는가 (음악이 멈추지 않는가)
3. 더미 API URL이 외부에서 응답하는가

---

## 7. 개발 산출물

| 항목 | 마감 | 담당 |
| --- | --- | --- |
| 시연용 dev build (팀 기기 설치 완료) | D4 마감 3h 전 | 김민서 |
| seed 데이터 주입된 시연 계정 | D4 마감 3h 전 | 김은송 |
| 설치 링크 (선택) | D4 마감 3h 전 | 김민서 |

설치 링크는 **시뮬레이션 모드가 동작할 때만** 낸다.
심사위원이 열어서 아무것도 안 움직이면 없는 것보다 나쁘다.

**시연 영상·발표 자료는 이 표에 없다.** 디자인 2명을 포함해 별도 논의로 정한다.

---

## 8. 매일 지키는 것

- 아침 10분 · 저녁 10분 동기화. 저녁엔 반드시 `main` 머지
- **저녁 동기화 직후 김민서가 재빌드·재설치 세션을 연다.** 다음 날 아침 각자 최신 빌드로 시작한다 (§1-1)
- `git pull --rebase && git push` — **빌드 깨진 채 push 금지**
- 문서와 코드가 다르면 **문서가 맞다.** 수치를 고칠 일이 생기면 `ENGINE.md`부터 고친다
- 남의 폴더는 직접 고치지 않고 주인에게 요청 (`AGENTS.md` §2)
